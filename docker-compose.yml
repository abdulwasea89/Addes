services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3
